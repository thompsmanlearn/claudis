# /agents/production

Proven agents currently active in n8n. Each agent has:
- `workflow.json` — n8n workflow with credentials replaced by `{{CREDENTIAL_NAME}}` placeholders
- `metadata.json` — agent_name, status, telegram_command, webhook_url, description, date promoted, lessons from building
