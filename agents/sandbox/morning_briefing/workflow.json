{
  "name": "SANDBOX - Morning Briefing",
  "workflow_id": "xt8Prqvi7iJlhrVG",
  "status": "sandbox",
  "last_updated": "2026-04-05",
  "description": "Daily Telegram briefing: work queue count+types, agent status counts, system health, 24h output count. No LLM calls. Promotion candidate pending Bill approval (sends Telegram = third-party write).",
  "fixes": [
    "2026-04-05: Empty-array chain halt resolved — Fetch Work Queue now POSTs to Supabase RPC get_pending_work_summary() which always returns {count, items} (1 item). Confirmed execution 2033 success.",
    "2026-04-05: Paired-item tracking — Build Message uses .first().json not .item.json across Code node chains."
  ],
  "webhook": "POST /webhook/morning-briefing",
  "supabase_rpc": "get_pending_work_summary() — returns JSON_BUILD_OBJECT(count, COALESCE(JSON_AGG(items), []))",
  "note": "Credentials replaced with placeholders. See ~/aadp/mcp-server/.env for actual keys."
}
