# /experiments

Structured tests with designed questions — distinct from `/attempts/` (which are build attempts).

**Naming:** `YYYY-MM-DD-hypothesis.md`

**Structure:** Hypothesis, method, expected outcome, actual result, confidence delta. An experiment has a designed question; an attempt has a build goal. Collapsed experiments lose the distinction between "I tried to build this" and "I ran a test to answer this question."
