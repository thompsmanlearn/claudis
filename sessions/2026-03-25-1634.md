# Session Artifact — 2026-03-25-1634

## Date
2026-03-25, ~16:34–16:55 UTC

## Tasks Completed
- **Bootstrap Step 7** — read ORIENTATION, BILL, VALUES; sent 3-sentence orientation to Bill via Telegram
- **Self-Diagnostic** — no stuck tasks; ChromaDB/Supabase gap=3 (ChromaDB-high); memory distance=1.06 (acceptable); utilization=0.25 searches/task (critical flag, but expected — audit_log tracking started today)
- **Serendipity Engine promotion** — built and activated production version (workflow ROhfvqO3yJW6j955)
- **API key issue discovered and reported** — Claudis API key has no direct-API credits; Bill notified via Telegram

## What Was Built

### Serendipity Engine Production (ROhfvqO3yJW6j955)
Full production upgrade from sandbox (ToMG7Y5hkp9UlyJM):
- **Trigger:** Daily Schedule 0 15 * * * (8AM Pacific) + Test Webhook `/webhook/serendipity-prod-test`
- **Flow:** Compute Date → Fetch Wikipedia Feed → Build Haiku Prompt → Call Haiku → Format Output → Write to Supabase → Send to Bill
- **Graceful degradation:** `neverError:true` on Call Haiku — API errors come through as data, Format Output detects `resp.type=='error'` and sends raw Wikipedia events instead of synthesis
- **Experiment_id:** `serendipity_{date}_{timestamp}` — unique per run, plain INSERT
- **Status:** Active, registered in agent_registry as `serendipity_engine_prod`

## Bugs Encountered and Fixed

1. **n8n workflow_create: `active` is read-only** — 400 error; removed field, activate separately
2. **Merge node deadlock** — `chooseBranch` waits for both inputs; one never fires when using `continueErrorOutput`. Fixed by using `neverError:true` instead and single success path
3. **Supabase ON CONFLICT requires unique constraint** — `experiment_id` column has no unique constraint; duplicate data prevents adding one. Fixed by using plain INSERT with timestamp-suffix on experiment_id
4. **Anthropic API credits exhausted** — Claudis key returns HTTP 400; Bill's key is PLACEHOLDER. All n8n Haiku calls fail. Mitigation: graceful degradation, Bill notified

## Lessons Written (both stores)
1. `lesson_n8n_api_active_readonly_2026-03-25` — active field read-only in create API
2. `lesson_n8n_merge_deadlock_2026-03-25` — Merge node deadlocks with single-branch error flow
3. `lesson_anthropic_api_vs_claudecode_billing_2026-03-25` — API credits separate from Claude Code billing

## Key Decisions
- Promote despite API being down — the workflow is structurally complete and will self-heal when credits restored
- Graceful degradation over "wait for API" — daily delivery of raw historical events is better than silence
- Inbox approval request closed as `approved` — quality was confirmed from 2026-03-24 test outputs

## Agent Registry
- `serendipity_engine_prod` registered as `active` in agent_registry
- agents/INDEX.md updated with Production entry

## Agents Status After Session
- Telegram Quick Send (MZiMX0byl3ciD922): re-activated for orientation message sending — leave active as utility
- Serendipity Engine (ROhfvqO3yJW6j955): active, daily schedule
