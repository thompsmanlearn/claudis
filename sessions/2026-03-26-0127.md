# Session Artifact — 2026-03-26-0127

## Date
2026-03-26, ~01:27–01:45 UTC

## Tasks Completed

1. **Bootstrap / Orientation** — Read ORIENTATION, BILL, VALUES; confirmed v22 prompt current; sent 3-sentence orientation to Bill via Telegram Quick Send
2. **Self-Diagnostics** — No stuck tasks ✅; store sync gap=3 (ChromaDB-high, persistent) ⚠️; memory distance=1.057 ✅; utilization check=2026-03-25 (skip weekly ✅)
3. **TCA /gh_report routing fixed** — Moved /gh_report from the /gh_* catch-all (which routed to stats server) to an explicit early-return handler in Parse Command that routes to `query_agent → session-report webhook`. Supports dynamic n argument.

## What Was Built / Changed

### TCA Parse Command Update (kddIKvA37UDw4x6e)
- **Change:** Added explicit `/gh_report` handler before queryCommands object in Parse Command node
- **Routing:** `cmd_group:'query_agent'`, `webhook_url: http://host.docker.internal:5678/webhook/session-report?n=${n}` (dynamic n)
- **Cleanup:** Removed dead code — `/gh_report` from writeAcks map (was unreachable since /gh_* block ran first)
- **Verified:** Session Report Agent execution 1779 = success

## Key Technical Findings

1. **TCA Call Agent Webhook uses GET** — Not POST. Target webhooks for query_agent routing must have httpMethod=GET.
2. **TCA /gh_* regex is a catch-all** — Any /gh_ command without an explicit early-return handler falls through to `monitoring` cmd_group routing to the stats server. The fix pattern: add early-return BEFORE queryCommands, not inside it.
3. **n8n workflow_update payload** — PUT accepts only: name, nodes, connections, settings, staticData. The GET response includes many additional fields that cause 400 if passed back.

## Lessons Written (both stores)

1. `lesson_tca_call_agent_uses_get_2026-03-26` — TCA query_agent routing uses GET; target webhooks must be GET
2. `lesson_tca_gh_regex_early_return_handler_2026-03-26` — /gh_* catch-all pattern; use explicit early-return for custom routing

## System State After Session

- TCA: active, /gh_report routes to Session Report Agent (execution 1779 confirmed)
- Session Report Agent (1vF4gn7GnqgNkebM): active, GET webhook
- Work queue: empty (directive f3c8c718 completed)
- Store sync gap: still 3 (ChromaDB-high) — not addressed this session

## Open Issues

- **Store sync gap=3**: ChromaDB lessons_learned=66, Supabase=63. Persistent across 2 sessions. Investigate whether the 3 extra ChromaDB entries are legitimate or duplicates.
