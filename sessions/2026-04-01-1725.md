# Session Artifact — 2026-04-01-1725
**Type:** Diagnostics + V2 compliance fix + health scan
**Collaborators:** Claudis (Claude Code, sentinel cycle)
**Duration:** ~45m

## Tasks Completed

1. **Bootstrap + Diagnose** — System healthy. No stuck tasks, store sync gap=1 (healthy), memory distance=1.057 (healthy), utilization check <6 days (skipped). Work queue empty.

2. **haiku_self_critic V2 upgrade** — Agent had ManualTrigger (not testable via API, V2 non-compliant). Replaced with Webhook trigger (path: haiku-self-critic). Update persisted in n8n. Deactivated after discovering webhook registration limitation.

3. **n8n webhook registration limitation discovered** — Workflows activated via API after n8n startup do NOT have their webhooks registered in the running process. Only workflows in the "Start Active Workflows" startup log have functional webhooks. This explains why haiku_self_critic (activated post-startup) returned 404 despite active=true. Fix: `docker compose restart n8n`. Lesson written to both ChromaDB and Supabase.

4. **agent_health_monitor scan** — Triggered successfully (was in startup list). Results: 10 agents checked, 1 issue: TCA has 1 stale error from 2026-03-29 (below 3-error retire threshold, event-driven so no recent executions expected).

5. **Inbox items written** — 3 items for Bill:
   - Approval request: promote wiki_attention_monitor
   - Approval request: promote github_issue_tracker
   - Recommendation: n8n restart to register 2 new webhooks

6. **Agent index + sandbox dir** — Created ~/aadp/claudis/agents/sandbox/haiku_self_critic/workflow.json (credentials stripped). Updated INDEX.md.

## Decisions Made

- **haiku_self_critic not testable this session** — needs n8n restart. Left deactivated (correct for sandbox). Will be testable after next n8n restart.
- **No promotions** — wiki_attention_monitor and github_issue_tracker both need Bill's approval (send Telegram). Inbox requests written.
- **TCA stale error is not a concern** — TCA is event-driven, last run was 2026-03-29 (Bill hasn't sent commands since). 1 error below retirement threshold.

## Key Finding

**n8n does not dynamically register webhooks for post-startup workflow activations.** This is a significant operational constraint: any sandbox agent that was inactive when n8n last started cannot be webhook-tested without a restart. The "Start Active Workflows" log at n8n startup is the authoritative list of what webhooks are actually live. This explains previous mysterious 404s on recently-built agents.

## Lessons Written

1. `lesson_n8n_webhook_dynamic_registration_2026-04-01` — n8n webhook paths not registered for post-startup activations; requires docker restart to fix; startup log is authoritative

## Commits

- (this session artifact commit)

## Usage

~35% session context (sentinel autonomous cycle)
