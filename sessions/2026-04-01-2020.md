# Session Artifact — 2026-04-01-2020
**Type:** Bill session — maintenance + security assessment
**Collaborators:** Claudis (Claude Sonnet 4.6) + Bill
**Duration:** ~60m
**Usage:** ~12% session context

## Tasks Completed

1. **Bootstrap** — developer_context_load, 4 identity docs, BECOMING, latest session artifact read. Session notes: 0 unconsumed (previous session's inbox items had been consumed).

2. **n8n restart** — `docker compose restart n8n` at `/home/thompsman/n8n/`. All post-startup webhooks now registered including haiku_self_critic and wiki_attention_monitor. Health check: `{"status":"ok"}`.

3. **Evaluated wiki_attention_monitor** — 4/5, recommendation: promote. Scores: behavior_consistency=4, output_quality=4, reliability=3, integration_fit=4. Bill approved. Promoted: agent_registry status→active, workflow activated in n8n, audit_log entry written, INDEX.md updated.

4. **Evaluated github_issue_tracker** — 3/5, recommendation: keep_sandbox. Root cause identified: audit log was chained after Telegram (if Telegram failed, audit never fired). Fixed: restructured connections so Filter Stale Issues branches to both Send Telegram AND Write to experimental_outputs in parallel. Audit log now fires unconditionally. Re-evaluated: still 3/5 keep_sandbox — remaining issues: GitHub token hardcoded in workflow header (needs n8n UI credential move), config externalization (3-day threshold, repo path hardcoded). Manual step documented for Bill.

5. **Supabase RLS security assessment** — Full assessment of 27 tables. Findings:
   - 17 tables with RLS fully disabled (anyone with anon key can read/write/delete)
   - 10 tables with RLS enabled but permissive public policies (effectively same exposure)
   - Highest sensitivity: agent_prompts (prompt_text), inbox (body, bill_reply), session_notes (content)
   - Key finding: ALL AADP access paths use service_role key (n8n confirmed via JWT decode; MCP server confirmed via .env). Service_role bypasses RLS unconditionally. Zero agent disruption from enabling RLS.
   - Fix plan: Phase 1 (17 ALTER TABLE ENABLE ROW LEVEL SECURITY), Phase 2 (DROP permissive public policies on 10 tables), Phase 3 (verify one agent write cycle). SQL written and ready.
   - Deferred to next session per Bill's usage constraint (14% weekly remaining).

## Decisions Made

- **wiki_attention_monitor promoted** — evaluator approved (4/5), Bill approved. First Bill-approved promotion of the session.
- **github_issue_tracker stays sandbox** — structural fix applied but GitHub token credential hygiene requires manual n8n UI step before re-evaluation and promotion.
- **RLS fix deferred** — 14% weekly usage remaining until Saturday. Assessment complete, execution next session.
- **Evaluator batch and agent builds** — deferred to next week per Bill's constraint.

## Key Finding

**Supabase RLS is the highest-priority infrastructure task for next session.** The fix is low-risk (service_role bypasses RLS unconditionally), the SQL is ready, and the advisory will clear immediately on execution. This should be the first thing done at next session start.

## Lessons Written

1. `lesson_supabase_rls_service_role_bypass_2026-04-01` — service_role always bypasses RLS; enabling RLS on AADP tables has zero impact on n8n workflows or MCP tools; anon key is the exposure vector

2. `lesson_n8n_audit_chain_decoupling_2026-04-01` — audit log nodes must branch from the data-processing node, not from the delivery node; if audit is chained after Telegram, a Telegram failure silently drops the audit record

## Commits

- agents/production/wiki_attention_monitor_workflow.json added
- agents/sandbox/github_issue_tracker/workflow.json updated (connection fix)
- INDEX.md updated (wiki_attention_monitor promoted, github_issue_tracker fix noted)
- session artifact (this file)

## Open Items for Next Session

1. **Execute RLS fix** (Phase 1 + 2 SQL ready — see session notes)
2. **github_issue_tracker GitHub token** — move to n8n credential store via UI, then re-evaluate and promote
3. **Evaluator batch** — deferred to next week
4. **Agent builds** — deferred to next week
