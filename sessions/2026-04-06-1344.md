# Session 2026-04-06-1344

**Type:** Infrastructure / Self-diagnostic  
**Triggered by:** Interactive (Bill)  
**Duration:** ~80 min  
**Usage:** ~session close (est. 40–50% session, weekly unknown — CC restart needed for accurate read)

---

## Tasks Completed

### 1. work_queue_update MCP bug diagnosed and fixed (server.py)
The `_work_queue_update` function was injecting a non-existent `updated_at` column into every PATCH body → 400 on all calls. Secondary bug: status check was `"completed"` but DB constraint requires `"complete"`. Both fixed in server.py. Pending Claude Code restart to activate.

### 2. session_notes_load consume=false fix verified live
Fix from prior session confirmed working: two consecutive calls both returned `{notes: [], consumed: false}`. Closed the work_queue agent_debug item.

### 3. session_launch work_queue item found already complete
Was already closed in same session it was created. No action needed.

### 4. work_queue_utilization self-diagnostic (task b905dff5 → complete)
Full audit of 32 queue items. Key findings:
- Queue is healthy: 27 complete, 0 failed, no stale claimed items
- Sentinel was NOT autonomously consuming queue items — queue is entirely session-driven
- Priority ordering in scheduler.sh was backwards (`priority.desc` = lowest priority first)
- No atomic claim step — race condition possible between concurrent sessions
- `claimed_at` almost never set — no in-progress trace

### 5. scheduler.sh: four fixes committed and pushed (a449a08)
1. `priority.desc` → `priority.asc,created_at.asc` — selects highest-priority items first
2. Shell-level atomic claim via Supabase PATCH before `claude -p` launches
3. Rate-limit recovery: captures claude output, detects limit message, releases task back to `pending`
4. MCP fix companion documented (server.py fix above)

Committed to `thompsmanlearn/claudis` at `a449a08`. File: `architecture/sentinel/scheduler.sh` (new — first time this file is tracked in the repo).

---

## Decisions

- session_notes category constraint allows only `priority`, `in_progress`, `observation`, `todo` — `hot_handoff` is not valid. Handoff notes use `priority` category.
- work_queue_update, session_notes_save, and other MCP write tools all 400 when Claude Code's connected MCP server is pre-fix (PID 382661, started 13:12). Workaround: supabase_exec_sql. Fix: restart Claude Code.
- scheduler.sh added to claudis repo under `architecture/sentinel/` — natural home given existing architecture/ structure.

---

## Lessons Written

3 lessons this session (see lessons_learned in Supabase + ChromaDB):
1. `lesson_work_queue_update_bugs_2026-04-06` — two bugs in _work_queue_update: updated_at column + wrong status enum
2. `lesson_sentinel_priority_ordering_2026-04-06` — priority.desc is backwards; asc selects highest-urgency
3. `lesson_atomic_claim_pattern_2026-04-06` — claim before worker starts, release on failure; prevents lost tasks

---

## Commits

- `a449a08` — fix(sentinel): four scheduler.sh fixes for autonomous work_queue consumption

---

## Open / Next Session

- Restart Claude Code to activate work_queue_update MCP fix
- 3 self_diagnostic items pending (prompt_caching_threshold, session_handoff, 4pillars_evaluator)
- agent_debug: add audit logging to daily_research_scout n8n workflow
- Watch next Sentinel log to confirm atomic claim is working
