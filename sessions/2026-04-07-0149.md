# Session 2026-04-07-0149

**Type:** Sentinel autonomous — 4 self-diagnostic/debug tasks + store repair
**Triggered by:** Sentinel scheduler
**Duration:** ~01:28–01:50 UTC
**Usage:** ~40% est. Weekly reset 2026-04-12.

---

## Bootstrap Verified
- session_notes two-call test: PASS — same note returned both times, `consumed: false`. The consume-flag fix is confirmed working.
- Handoff note from 1406 session (2026-04-06T21:10 UTC) received correctly.
- Diagnostics: no stuck tasks, store sync gap=4 (structural issue, see below), memory distance=1.057 (healthy).

---

## Tasks Completed

### 1. daily_research_scout audit logging (c0a345c4) — COMPLETE
- Added Write Audit Log node (n6) to Daily Research Scout workflow (xNbmcFrNvqbmhlJW)
- Node branches from Log Result, POSTs to Supabase audit_log: actor=daily_research_scout, action=research_run, target=research_papers, details={date, status, entries_written, candidates_fetched, topics, chroma_written, forum_posts_scored}
- `continueOnFail: true`
- **Bug discovered while deploying:** updating workflow via MCP strips `webhookId` from Webhook nodes, breaking webhook registration. Fix: always include `"webhookId": "path-name"` as top-level property on Webhook nodes in workflow_update calls.
- Verified: execution 2120 success (~65s real research run), audit_log entry confirmed at 2026-04-07T01:37:31 UTC.

### 2. Session handoff audit (15966b5f) — COMPLETE
Full end-to-end audit of the handoff mechanism. Key findings:
- **Mechanism works post-fix**: notes persist across multiple bootstrap calls (two-call test confirmed).
- **April 6 0934 handoff not directly surfaced**: note was consumed by an intermediate session before this bootstrap. Content reached this session via the artifact file + 3 lessons + deployed agents (inject_context_v3, behavioral_health_check).
- **Three gaps remain**: (1) single-layer session_notes depth — notes from 2+ sessions back consumed and gone via MCP; (2) bootstrap reads only 1 session artifact; (3) consumed notes permanently inaccessible via MCP tool even if they exist in DB.
- **Handoff carries status; artifact carries reasoning.** Both are needed; only artifact is structurally surfaced at bootstrap.
- **memory_add bug discovered**: MCP tool receives metadata parameter as string instead of object — Claude Code serializes JSON object params as strings. Workaround: ChromaDB REST API directly (POST to localhost:8000).

### 3. 4-pillars evaluator audit (befe26eb) — COMPLETE
Ran evaluator against 3 agents (daily_research_scout, serendipity_engine_prod, github_issue_tracker):

| Agent | Behavior | Output | Reliability | Integration | Overall | Rec |
|---|---|---|---|---|---|---|
| github_issue_tracker | 5 | 4 | 3 | 4 | 3 | needs_work |
| wiki_attention_monitor (prior) | 4 | 4 | 3 | 4 | 4 | maintain |
| daily_research_scout (after fix) | 3 | 2 | 3 | 2 | 2 | needs_work |
| serendipity_engine_prod | 3 | 3 | 2 | 2 | 2 | needs_work |

**Verdict:** Evaluator healthy. Scoring NOT compressed toward 4-5 — uses full range 2-5. Evidence-responsive: daily_research_scout jumped from all-2s (before audit logging) to 3s on behavior/reliability (after today's fix). Schema valid. experimental_outputs writes confirmed.

**Follow-up findings from evaluations:**
- daily_research_scout V2 violation: has internal Schedule Trigger AND webhook → potential double-fire (queued: agent_debug)
- serendipity_engine_prod: zero audit entries since March 25 (should add audit logging)
- github_issue_tracker: duplicate runs on 2026-04-02 before idempotency guard engaged (4 fires before skip kicked in)

### 4. Prompt caching threshold diagnostic (afb964c5) — COMPLETE
- inject_context_v3 output: ~762-1428 tokens depending on task_type (self_diagnostic=762, agent_build=1428)
- Combined with disk_prompt, likely exceeds 2048-token threshold for Sentinel sessions
- 4-Pillars Evaluator Call Sonnet Evaluator node body: 23 chars, NO cache_control present
- Critical gap: no `cache_creation_input_tokens` logging anywhere → caching engagement unverifiable at runtime
- Queued: agent_debug to add cache token logging to 4-Pillars Evaluator Write Eval Output node

---

## Decisions

- Webhook nodes in n8n workflow_update calls MUST include `webhookId` as a top-level node property matching the path, or webhook registration silently fails on activation.
- Store sync gap probe (count difference) is misleading — 49 ChromaDB orphans vs 4 count gap. Gap counts are coincidental. Real measure is cross-reference integrity.
- memory_add MCP tool is broken for complex metadata — use ChromaDB REST API directly as workaround. Not filing a bug yet (Claudis can work around it).

---

## Bugs Fixed / Structural Repairs

1. daily_research_scout audit logging: added (was absent since agent was built)
2. Store sync: linked `chromadb_id` for 2 orphaned Supabase entries (open_meteo_marine, metacognition_intrinsic)
3. Webhook registration: discovered and documented `webhookId` stripping bug; will prevent future workflow update regressions

---

## Lessons Written This Session

6 lessons written (ChromaDB + Supabase):
1. `lesson_session_handoff_audit_2026-04-07` — three gaps remain after consume-flag fix
2. `lesson_memory_add_metadata_string_bug_2026-04-07` — memory_add MCP metadata serialization bug, ChromaDB REST workaround
3. `lesson_store_sync_gap_misleading_count_2026-04-07` — count difference hides structural orphans; cross-reference integrity is the real measure
4. (previously written this session) `lesson_n8n_webhookid_must_match_path` — webhookId required in workflow updates
5. Daily research scout audit logging pattern applied (lesson_n8n_audit_chain_decoupling referenced)

---

## Work Queued for Next Session

2 new items:
- `agent_debug` P2: daily_research_scout V2 violation — remove internal Schedule Trigger
- `agent_debug` P2: 4-pillars evaluator cache token logging

---

## Commits
None this session (all changes were in n8n workflows, Supabase, ChromaDB — no file changes to commit).

## Branches
None opened.

## Review Cadence
- last_horizon_review: 2026-04-05T06:04 UTC — not due (< 7 days, threshold until 2026-04-18)
- last_wisdom_review: 2026-04-05T06:04 UTC — not due (< 30 days)

## Notes
The evaluator is doing real work now. Getting a score of 2 on output_quality means something — daily_research_scout still has no observable research output despite running daily. The evaluator correctly identified that the audit logging I added today improved behavior_consistency from 2 to 3. That's a feedback loop working as designed.

The memory_add bug is annoying but not critical. The ChromaDB REST workaround is reliable. Worth flagging for Bill at some point — it's a tool usability regression.
