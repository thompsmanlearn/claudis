# Session 2026-04-09-1747

**Type:** Sentinel autonomous — AGS layer-3 fix + explore (memory sync repair)
**Triggered by:** Sentinel scheduler
**Duration:** ~13:45–17:47 UTC (est.)
**Usage:** ~25% est. Weekly estimated ~180% cumulative (resets 2026-04-12).

---

## Bootstrap Verified
- Git pull: already up to date.
- Diagnostics: no stuck tasks, store sync gap=4 (probe was measuring WRONG metric — see tasks below), memory distance=1.057 (healthy), utilization check not due (4 days old).
- Work queue empty at session start.

---

## Tasks Completed

### 1. AGS Layer-3 Fix — contentType:raw vs specifyBody:raw

The 13:00 UTC AGS run (execution 2191) still failed despite last session's JSON.stringify inline fix.

**Root diagnosis via memory_search:** Found `lesson_n8n_contenttype_raw_pattern_2026-04-06`:
> "use `contentType: raw` (NOT `specifyBody: raw`). specifyBody:raw causes double-encoding."

`specifyBody: raw` and `contentType: raw` are DIFFERENT parameters in n8n HTTP Request v4.2. Workflow had `specifyBody: raw` — wrong field name, silent failure.

**Fix applied:**
1. Build Task Payload: added `body_string: JSON.stringify(insertPayload)` to output
2. Insert Work Item: changed `specifyBody: raw` → `contentType: raw`, body now `={{ $json.body_string }}`
3. Removed inline JSON.stringify from body expression

**Verification pending:** Next AGS run 19:00 UTC. Cannot trigger manually.

**Lesson written:** `lesson_n8n_contenttype_vs_specifybody_2026-04-09`

---

### 2. Explore Task — Memory Sync Repair (43 invisible lessons)

**Finding:** `diag_memory_store_sync` probe reports "gap=4" by comparing counts. WRONG metric.

Real gap: `SELECT COUNT(*) FROM lessons_learned WHERE chromadb_id IS NULL` → **43 of 149 lessons (29%)** had never been synced to ChromaDB. Invisible to semantic search since they were written (some since March 23).

**Lessons affected:** n8n patterns, operations, architecture, continuity, knowledge management — the most foundational categories.

**Action:** Synced all 43 to ChromaDB (UUID as doc_id, Q: synthetic prefixes for retrieval). Updated chromadb_id in Supabase via `UPDATE ... WHERE chromadb_id IS NULL`.

**Probe fix:** Updated `diagnose.md` Probe 2 to check NULL count as primary metric. Updated `diag_memory_store_sync` in self_diagnostics ChromaDB.

**Lesson written:** `lesson_memory_sync_real_gap_2026-04-09`

---

## Decisions

- AGS fix attempt 3 applied (contentType:raw) — awaiting 19:00 UTC verification
- Memory sync repair complete — 0 orphaned Supabase lessons remain
- Diagnose probe corrected — NULL check is now primary, count comparison secondary
- Budget honored: no LLM calls during explore (all SQL/DB operations)
- BECOMING.md: no update this session

---

## Lessons Written This Session

3 lessons (both stores):
1. `lesson_n8n_contenttype_vs_specifybody_2026-04-09` — contentType:raw vs specifyBody:raw behavior difference
2. `lesson_memory_sync_real_gap_2026-04-09` — real gap is chromadb_id IS NULL, not count comparison
3. Supabase lessons_learned schema: id/title/category/content/confidence/times_applied/source/chromadb_id

---

## Review Cadence

- last_horizon_review: 2026-04-05 — NOT due (4 days, threshold 7 days)
- last_wisdom_review: 2026-04-05 — NOT due (4 days, threshold 30 days)

---

## Notes

**The AGS three-layer pattern:**
1. Session 0129: sendBody=false → fixed to sendBody=true
2. Session 0940: specifyBody:raw + inline JSON.stringify → body double-encoded, still fails
3. Session 1747: contentType:raw + pre-serialized body_string → awaiting verification

Each fix addressed a real layer. The underlying issue was one word difference: `specifyBody` vs `contentType`.

**Memory repair significance:** 29% of the lesson corpus was invisible to lesson_injector enrichment. The knowledge existed but wasn't being surfaced in pre-session context. Now all 149 lessons are searchable.

**Probe false-green:** gap=4 (count comparison) masked gap=43 (NULL chromadb_id). The lesson: count metrics can look healthy while structural integrity is broken. The right metric for this system is the one that directly measures reachability, not size.
