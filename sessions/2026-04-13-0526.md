# Session: 2026-04-13-0526
**Type:** Bill-initiated (context-continued from previous session)
**Branch:** roblox-lune-install
**Commits:** 21dea77

---

## Tasks Completed

1. **disk_prompt.md → v29** — Context deduplication pass. Removed "What This System Does", "Operating Orientation", "Values and Conventions", "Service Endpoints" (all now authoritative in foundation docs). Fixed Bootstrap Step 1 (DCL does not retrieve the prompt). Trimmed change notes header to v28+v29 only. 497 → 407 lines (~747 tokens removed). Pushed to Supabase (agent_prompts v29), updated master_prompt_backup.txt, archived to architecture/prompts/v29.md.

2. **DCL size audit and optimization** — Measured actual section sizes. Found session_notes was 32,573 chars (~8,143 tokens) due to 17 unconsumed notes since April 6. Fixed four things:
   - Consumed all 17 stale notes immediately (instant ~8,143 token recovery)
   - Fixed bootstrap.md Step 2: now explicitly requires `session_notes_load(consume=true)` with explanation of consequences
   - Added DCL notes hard cap: 5 most recent only (server.py safety net)
   - Added agent_registry column projection: stripped 9 unused fields per agent (~3,128 tokens saved structurally)

3. **Lessons written** — session_notes accumulation pattern written to ChromaDB lessons_learned and Supabase lessons_learned.

---

## Capability Delta

**What the system can do now that it couldn't at session start:**

- **Bootstrap consumes notes correctly** — sessions no longer silently accumulate stale handoff context. The consume=true requirement is now documented with consequences in bootstrap.md.
- **DCL is 73% lighter** — steady-state ~4,025 tokens vs 14,890 tokens before. Every sentinel session starts with significantly more usable context budget.
- **disk_prompt is procedures-only** — v29 contains no content duplicated from foundation docs. CONTEXT.md, CONVENTIONS.md are the single source of truth for system facts and values.

---

## Key Decisions

- Consume bug root cause: `session_notes_load` defaults to `consume=false` (safe peek). Bootstrap never overrode this. Pattern: "safe by default" tools require explicit opt-in at call site.
- DCL notes capped at 5 (not 3) — allows one note per session for up to 5 sessions before the cap triggers, covering weekend gaps.
- Bootstrap.md fix documents *why* (not just what): "Without consume=true, notes pile up ~1,700 tokens/session" — so future instances understand the consequence, not just the rule.

---

## Lessons Written

1. **session_notes accumulation — safe-default tool requires explicit consume=true at call site** (ChromaDB: lesson_session_notes_consume_pattern, Supabase: lessons_learned)

---

## Usage

Session % and weekly %: not provided this session.
