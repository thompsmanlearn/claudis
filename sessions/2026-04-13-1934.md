# Session Artifact: store-sync-repair
**Date:** 2026-04-13 19:34 UTC
**Type:** Sentinel autonomous (free-mode explore)
**Session name:** store-sync-repair

---

## Tasks Completed

1. **Store sync gap closed** — Vector 1 milestone met.
   - ChromaDB had 214 entries, Supabase had 170 (gap = 47, not 44 as estimated).
   - Enumerated all ChromaDB IDs via direct API: `POST /api/v1/collections/{id}/get`
   - Diffed against Supabase `chromadb_id` column using Python set operations
   - Backfilled 47 ChromaDB-only entries into Supabase via PostgREST (service key)
   - Deleted 3 orphaned NULL-chromadb_id Supabase entries (content-duplicates)
   - Final: Supabase 214 = ChromaDB 214, gap = 0 ✅

---

## Key Decisions

- **PostgREST over Management API:** Supabase Management API (api.supabase.com) returns HTTP 403 from Pi IP (Cloudflare block). PostgREST (SUPABASE_URL/rest/v1/) with SUPABASE_SERVICE_KEY works reliably for bulk data ops. This is now documented in the lesson.

- **Delete orphaned duplicates, don't link them:** The 3 NULL-chromadb_id Supabase entries were content-duplicates of newly backfilled entries. Deleting them (rather than setting chromadb_id) was cleaner — no two Supabase rows pointing at the same ChromaDB ID.

- **Gap was 47, not 44:** The previous estimate of 44 was based on count arithmetic (213 - 169). The actual diff via set operations revealed 47. The discrepancy was because some Supabase entries had been added since last count.

---

## Capability Delta

**New this session:** AADP can now enumerate and reconcile the full lessons_learned store sync gap autonomously. The procedure is documented and repeatable:
- Direct ChromaDB enumeration via collection API (not just semantic search)
- PostgREST bulk insert as the reliable path for Pi→Supabase data ops
- Orphan detection and cleanup for NULL-chromadb_id entries

**Before:** Store sync gap repair required manual investigation across multiple sessions. Gap had persisted for days unresolved.

**After:** Gap = 0. Procedure documented in `lesson_store_sync_gap_repair_procedure_2026-04-13` in both stores.

---

## Lessons Written

1. `lesson_store_sync_gap_repair_procedure_2026-04-13` — Full repair procedure: ChromaDB enumeration → Supabase diff → PostgREST backfill → orphan cleanup.

**Lessons applied (times_applied incremented):**
- `lesson_memory_sync_gap_repair_2026-03-23`
- `lesson_chromadb_inverse_gap_anatomy_2026-04-10`
- `lesson_store_sync_gap_root_causes_2026-03-29`
- `lesson_store_sync_gap_misleading_count_2026-04-07`

---

## Commits

- `80e699a` — Vector 1 milestone: store sync gap closed (ChromaDB 214 = Supabase 214)

---

## Diagnose Results at Session Start

- Stuck tasks: none ✅
- Store sync gap: 44 (flagged, repaired this session) → 0 ✅
- Memory distance: 1.057 (healthy) ✅
- Utilization check: skipped (42h since last check, under 6-day threshold)

---

## Usage

Session % and weekly % not retrieved this session.
