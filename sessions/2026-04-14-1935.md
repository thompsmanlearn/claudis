# Session Artifact: 2026-04-14-1935

**Type:** Sentinel autonomous — explore (free mode)
**Duration:** ~45 minutes
**Context budget:** ~35% estimated

---

## Tasks Completed

- `b072f53b` — explore (free mode) → **complete**

---

## Key Decisions

1. **Target: retrieval_log wiring.** Three separate lessons (times_applied 0 for 3+ weeks) flagged the gap. Chose this over researching zero_applied regression because it was actionable, constrained (no n8n API needed), and closes a structural gap the system had been surfacing without acting on.

2. **Fire-and-forget bulk insert pattern.** Used same approach as `audit()` — `asyncio.ensure_future(log_retrieval(...))` so search latency is never affected. Bulk PostgREST insert (list of dicts) more efficient than one row per result.

3. **was_relevant stays NULL.** No labeling mechanism yet. Collecting unlabeled data first is correct — labeling design is a separate concern.

4. **Did not restart MCP server.** Change is on disk and will take effect on next session start. Restarting mid-session would break all MCP tools.

---

## Capability Delta

**New:** retrieval_log now accumulates query-document pairs on every `_memory_search` call (effective next MCP server restart). This starts the data collection pipeline toward ChromaDB adapter training — the only path to measurable long-term retrieval accuracy improvement.

**Before this session:** retrieval_log table existed with correct schema, 0 rows, and nothing writing to it.  
**After this session:** `log_retrieval()` wired into server.py. Next MCP restart begins accumulation.

---

## Lessons Written

1. `lesson_retrieval_log_wired_2026-04-14` — retrieval_log wired: table existed with 0 rows, added log_retrieval() fire-and-forget to _memory_search

---

## Flags and Observations

- **zero_applied regression:** 96 → 142 (up 46). 65% of 218 lessons have never been applied. Flagged in TRAJECTORY.md Vector 2. Root cause investigation is the next milestone for that vector.
- **n8n API key still expired.** Bill action required. Webhooks functional; execution_list/workflow_get blocked.
- **server.py not in git.** Change is disk-only. Should evaluate adding mcp-server to version control in a future session.

---

## Commits / Branches

- TRAJECTORY.md updated and committed (this session)
- No n8n workflow changes (API key expired)
- server.py edit: disk-only, not in git

---

## Usage

~35% session context budget estimated. Weekly % not tracked.
