# Session Artifact: 2026-04-15-0335

**Session type:** Sentinel autonomous — explore (free mode)
**Duration:** ~70 minutes
**Context budget:** ~35% consumed
**Weekly %:** Not tracked (no reset timestamp in system_config)

---

## Tasks Completed

1. **Bootstrap + diagnose** — all 4 probes passed: no stuck tasks, store sync gap=0 (219/219), memory distance=1.057 (healthy), utilization probe skipped (last check 3 days ago).

2. **Zero_applied regression diagnosis** — full investigation of why times_applied=0 for 128/219 lessons (62%).
   - Distance scan on 10 recent zero_applied lessons: all healthy (0.36–0.78). Embedding NOT the issue.
   - Mechanism check: inject_context_v3 calls increment_lessons_applied_by_id RPC → confirmed working.
   - Root cause 1: **velocity gap** — 55 lessons created/week, ~35 retrieval increments/week. Net +20 zero_applied/week.
   - Root cause 2: **niche topic invisibility** — 25 lessons older than 21 days covering one-time scenarios (Wikipedia API, Semantic Scholar, FRED debug, serendipity eval) that haven't recurred.

3. **inject_context_v3.1 deployed** — zero_applied wildcard slot added to stats_server.py:
   - Fetches up to 50 zero_applied candidates (>3 days old, chromadb_id not null) from Supabase
   - Deduplicates against semantically-retrieved lessons
   - Random sample of 2 added to context as "Uncirculated Lessons" section (300 chars each, last section so trimmed first if over budget)
   - Added to lesson_ids for increment_lessons_applied_by_id tracking
   - Version bump: v3.0 → v3.1
   - Stats server restarted. n8n lesson_injector webhook tested: confirmed working.

4. **retrieval_log confirmed live** — Yesterday's server.py change (log_retrieval() fire-and-forget) is active. Rows accumulating from this session's memory_search calls.

---

## Key Decisions

- **Wildcard approach over staleness penalty modification**: The staleness penalty can't differentiate zero_applied from well-applied lessons (metadata doesn't include times_applied). The wildcard approach uses Supabase (which has times_applied) as the source of truth, avoiding this limitation.
- **2 wildcards per session, 300-char truncation**: Minimal context impact while ensuring coverage. Added last in sections list so token-budget trimming removes them before task-relevant context.
- **Deduplication against semantic results**: Prevents double-counting and double-increment for lessons that happen to match both semantic query and zero_applied pool.

---

## Capability Delta

**What the system can do now that it couldn't at session start:**
- inject_context_v3.1 guarantees eventual first-retrieval for ALL lessons regardless of topic diversity. Previously, lessons covering niche scenarios that never recurred were permanently invisible.
- retrieval_log is now accumulating query-document pairs — foundation for future ChromaDB adapter training (target: +70% retrieval accuracy at 1,500 labeled pairs).

---

## Lessons Written

2 lessons written to both ChromaDB and Supabase:
1. `lesson_zero_applied_root_cause_2026-04-15` — Root cause: velocity mismatch + niche topic invisibility
2. `lesson_inject_context_v31_wildcard_2026-04-15` — Implementation details of the wildcard fix

---

## Metrics

- zero_applied: 142 → 128 (14 lessons circulated this session)
- Validation target (<130): **PASSED** (128)
- Lessons in system: 219 Supabase / 219 ChromaDB (gap=0)
- Stats server: restarted, v3.1 active
- n8n API key: still expired (Bill action required)

---

## Open Items for Next Session

1. Monitor zero_applied — confirm downward trend over next 2 sessions
2. Wisdom-review targeting 25 old zero_applied niche-topic lessons — retire obsolete ones
3. Evaluate adding stats_server.py to version control (disk-only changes are brittle)
4. architecture_review agent (7mVc61pDCIObJFos) eligible for promotion once n8n API key renewed
