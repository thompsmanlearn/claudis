# Session — 2026-06-28-0029
Type: Interactive (directive from DIRECTIVES.md)
Directive: Project Node: Implement status strip error indicator badge (3c1d70dd)

## Tasks Completed

- Built `get_error_log_status` callable in uplink_server.py — single PostgREST call with `Prefer: count=exact` returning `{unresolved_count, recent[3]}`
- Added `_home_error_lbl` Label to Home tab status strip (`_build_home_layout`)
- Added `_load_error_status()` loader wired into `refresh_data()`
- Updated `_update_home_status_strip()`: `✅` when zero, `🔴 N` when non-zero
- Wrote and committed lean session artifact (`sessions/lean/2026-06-27-error-indicator-badge.md`)

## Key Decisions

- Used `Prefer: count=exact` (single request) rather than two separate REST calls — consistent with 6 existing usages in uplink_server.py
- Badge shows emoji only (no separate error list panel) — matches spec UI contract; expanded panel is future work

## Capability Delta

**Before:** Home tab status strip had no error log visibility.
**After:** Status strip shows `✅` (zero unresolved errors) or `🔴 N` (N unresolved). Refreshes on every `refresh_data()` call.
**Reader:** Bill via Anvil dashboard Home tab.

## Commits

claudis: c49c00f (attempt), 2a6113d (merge), 306c588 (artifact), 679baa7 (TRAJECTORY)
claude-dashboard: 0cc80e0 (attempt), b87d6a4 (merge)

## Lessons Written

0 (existing PostgREST count=exact pattern already documented in the file; no novel lesson)

## Usage

Session % and weekly %: not tracked in interactive mode.
