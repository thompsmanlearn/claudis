# Session: 2026-04-18 — B-040 Memory Tab

## Directive
B-040: Add Memory tab to Anvil dashboard — ChromaDB browser and curated Supabase views.

## What Changed

**anvil/uplink_server.py** — 5 new callables:
- `get_collection_stats()` — lists all ChromaDB collections with document counts
- `browse_collection(name, limit, offset)` — paginated docs with metadata (never includes embeddings — avoids IndexError at scale)
- `search_collection(name, query)` — semantic search returning docs + distances
- `delete_document(collection, doc_id)` — deletes from ChromaDB; also removes matching Supabase row when collection is `lessons_learned`
- `get_table_rows(table, limit)` — curated views for `research_papers` and `error_log`

**client_code/Form1/__init__.py** — Memory tab added:
- Tab button row: Fleet / Sessions / Lessons / Memory
- All `_show_*_tab` methods updated to hide/show memory panel
- ChromaDB browser: collection picker buttons (name + count), paginated doc list (15/page, prev/next nav), semantic search input, per-doc expand + delete
- Supabase browser: Research Papers and Error Log buttons with rendered card views
- State: `_memory_selected_coll`, `_memory_offset`, `_mem_page_size=15`, `_memory_loaded`

**Service:** `aadp-anvil.service` restarted — confirmed connected at 22:04:01.
Both repos committed and pushed (claudis main 809a341, claude-dashboard master a911c3f).

## What Was Learned
- Anvil `get_components()` works to check if a FlowPanel has children before adding it to the layout (pagination nav conditional).
- `error_log` curated view uses `resolved=eq.false` filter — if the column doesn't exist the call raises; worth verifying live.

## Unfinished
- Live test from the browser not performed — Bill should open the Memory tab, select a collection, confirm docs load, and test delete on a safe doc.
- `error_log` table column names (`workflow_name`, `error_message`, `resolved`) assumed from card spec — verify against actual schema if the tab throws on the Error Log view.
