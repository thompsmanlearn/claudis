# Session: 2026-04-18 — b-032-mobile-ux

## Directive
B-032: Dashboard mobile UX — collapsible sections, agent filtering, and protected indicators.

## What Changed
- `~/aadp/claudis/anvil/uplink_server.py`: added `protected` field to `get_agent_fleet()` select
- `~/aadp/claude-dashboard/client_code/Form1/__init__.py`: full rewrite with:
  - `_make_section()` helper — all 5 sections collapsible, System Status open by default
  - Agent fleet grouped by status with count badges, each group independently collapsible
  - Compact agent cards (name + status icon + ⚠️ protected mark) that expand on tap for detail/controls
  - Search/filter TextBox at top of Agent Fleet — filters cards by name on keystroke
  - `get_agent_fleet()` now returns `protected` field; ⚠️ rendered if true
- aadp-anvil.service restarted; uplink reconnected
- Both repos pushed (claudis main, claude-dashboard master)

## What Was Learned
- Anvil show/hide collapsible pattern uses `panel.visible = bool` — works cleanly with button text toggle
- All closures in event handlers need `make_*` factory functions to capture loop variables correctly
- Branch-per-attempt then fast-forward merge works cleanly for both repos

## Unfinished
Nothing. All "Done when" criteria met.
