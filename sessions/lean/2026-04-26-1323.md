# Session 2026-04-26-1323

**Type:** Lean — Bill-directed, no card pre-queued  
**Branch:** attempt/context-research-expand-sources → merged main (ce7a858 → 9f80f4f)

---

## Tasks completed

1. **Closed 2 stale work_queue items** — `explore/free` (11 days old, no consumer) and `design_and_build` for LEAN_BOOT stale-directive fallback (already implemented in step 5).

2. **Fixed empty-fetch behavior** — `context_engineering_research` now skips items where page fetch returns empty instead of inserting a thin title-only row. Error is still logged; item is not written.

3. **Expanded research sources from 2 → 6** — Added `_fetch_devto`, `_fetch_medium`, `_fetch_github`, `_fetch_lobsters` to stats_server.py.
   - dev.to: tag-based API (`agents`, `n8n`, `llmops`, `rag`, `claude`)
   - GitHub: repository search API, uses repo description as content (no page fetch)
   - lobste.rs: tag feed JSON API (`ai`, `ml`, `programming`)
   - Medium: RSS tag feeds (paywall pages skip cleanly via empty-fetch logic)

4. **Added per-source cap** — Each source limited to 5 candidates before global cap (20) is applied. Prevents any single source dominating the insertion budget.

5. **Acted on agent_feedback** — `context_engineering_research` feedback ("expand beyond HN+arXiv") processed and marked done.

6. **Resolved error_log** — `build.withpi.ai` fetch_error resolved; root cause was missing skip logic now fixed.

---

## Key decisions

- **dev.to tags narrowed after first run** — Broad tags ("ai", "machinelearning") yielded ergonomic chairs and browser-tab humor. Switched to domain-specific tags. One-iteration learning loop.
- **GitHub uses description, not page fetch** — Repo descriptions are rich enough for Haiku summarization; scraping github.com HTML would be noisy. Treated same as arXiv (SKIP_FETCH_SOURCES set).
- **PER_RUN_CAP raised 10→20** — With 6 sources × 5 cap = up to 30 candidates, 20 gives all sources a fair shot per run without excessive Haiku calls.
- **Medium kept despite paywall rate** — The skip-on-empty logic handles it gracefully; any open Medium posts that come through are bonus signal.

---

## Capability delta

**Before:** context_engineering_research pulled from HN + arXiv only; empty page fetches inserted thin rows (title only, noisy Haiku summary).

**After:** 6 sources with balanced representation; empty fetches are silently skipped; GitHub repos and lobste.rs community picks are now in the research feed. 58 total research_articles (was 16 at research micro-version close).

---

## Verification

Final run result: `inserted=15, skipped_dupe=26, capped=0, errors_logged=5`  
Sources in last run: dev.to (5), github.com (5), lobste.rs submissions (4), arxiv.org (1)  
Medium: 5 errors logged (paywall empty-fetch) — skipped cleanly, no thin rows.

---

## Lessons written: 2

1. `lesson_devto_tag_specificity_2026-04-26` — dev.to broad tags return popular/off-topic content
2. `lesson_per_source_cap_multi_fetch_2026-04-26` — per-source cap pattern for multi-source pipelines

---

**Usage:** session ~10%, weekly ~95%
