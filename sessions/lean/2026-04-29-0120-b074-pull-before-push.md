# Session: B-074 Option A — close-session pull-before-push
**Date:** 2026-04-29 01:20 UTC
**Type:** Lean — card execution (micro-session, direct fix)
**Branch:** none — doc change, direct to main

---

## Tasks completed

1. **Edited close-session.md** — added pull-before-push instruction block at the top of the GitHub steps section. Runs once per session (Step 1, or Step 3 if 1–2 skipped). If rebase fails, Telegram Bill and stop. Version bumped to v30.

2. **Exercised the new rule** — ran `git pull --rebase` before committing close-session.md itself. Discovered: pull fails with "unstaged changes" if called before staging. Must stage (or commit) first. The instruction placement (after staging is natural for step 1/3) makes this a non-issue in normal use.

3. **Pushed** — `78b1b21` on main. B-074 Option A complete.

---

## Key decisions

- Direct-to-main (no branch): close-session.md edit is a single-file doc change — qualifies under CONVENTIONS.md "direct-to-main for docs."
- Option B (Anvil pre-flight check) remains on the card as optional — not implementing now; Option A eliminates the common path.

---

## Capability delta

**New since session start:**
- close-session ritual now pulls --rebase before first push, eliminating the claudis divergence failure mode that caused manual boot recovery in B-070.

---

## Lessons written

0 new lessons — the divergence root cause lesson was already written during B-070 close (`lesson_claudis_divergence_git_pull_rebase_2026-04-29`).

---

## Branches

None opened.

## Commit hashes

- `78b1b21` — close-session v30: pull-before-push
- `2817158` — TRAJECTORY.md update

---

## Usage

session ~%, weekly ~%
