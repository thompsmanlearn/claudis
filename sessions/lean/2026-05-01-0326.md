# Session: 2026-05-01-0326

**Type:** Lean — directive execution
**Card:** B-075 Add-to-thread affordances
**Branch:** attempt/b075-add-to-thread → merged to master
**Commit:** 771b317 (claude-dashboard)

## Tasks completed

- Implemented "Add desktop analysis" section in Threads tab expanded card (`_build_thread_actions`): TextArea (height=120), entry_type DropDown (analysis/annotation/conclusion, default analysis), "Add as analysis entry" button, inline feedback label. Calls `add_thread_entry` with source=desktop_claude, embed=True. Clears TextArea on success; shows ⚠️ on empty submit.
- Implemented "Add to thread" button on Research tab article cards (`_build_research_article_card`): inline picker_panel (hidden by default, toggled by button). Picker calls `get_threads('active')` (existing callable), shows DropDown of active thread titles sorted by last_activity_at desc, defaulting to most-recent. Confirm writes gather entry with formatted content block (title, url, source, summary, rating, comment) and source=research_articles:{article_id}, embed=True. Visual "✅ Added to {title}" on success; picker hides. No-active-threads state shows correct disabled message.
- 106 lines added to Form1/__init__.py. No new uplink callables. No schema changes.
- Merged to master, pushed to GitHub. Site regenerated.

## Key decisions

- Used toggle-to-dismiss on "Add to thread" button (second click hides picker) rather than a modal — keeps article cards clean and avoids Anvil modal complexity.
- Reused `get_threads(state='active')` from B-071 — already returned sorted by last_activity_at desc, exactly the right default for the picker.
- Applied `_make_add_to_thread` / `_make_pick` factory function pattern (lesson_anvil_client_closure_pattern_2026-04-18) to avoid loop variable capture in article card rendering.

## Capability delta

**New:** Bill can paste desktop Claude analysis directly into a thread from the Threads tab expanded card. Bill can promote Research tab articles into any active thread with a two-click affordance. Both surfaces embed content into ChromaDB (embed=True). Thread provenance is now trackable by source field (desktop_claude vs research_articles:{id}).

## Lessons written

0 new lessons (patterns already captured in lesson_anvil_action_closure_pattern_2026-04-29 and lesson_anvil_client_closure_pattern_2026-04-18)

## Usage

session ~%, weekly ~%
