# Session: B-119 Auto-wiring
Date: 2026-05-09
Type: lean
Card: B-119
Code commits: e92f1cc, 3fda313, 25d58c7 (claudis); dce7c74 (claude-dashboard)

## Tasks Completed
- B-119: Auto-wiring agents to threads on charter save

## What Was Built
Added `capability_tags text[]` to `agent_registry` (DDL via supabase_exec_sql). Seeded tags for all 10 agents (9 active + thread_research_agent sandbox). Added `_score_agent()` and `_auto_wire_thread()` to `uplink_server.py`. `save_charter()` now calls `_auto_wire_thread()` after writing and returns `{**thread, '_auto_wire': result}`. Form1 unpacks the result and shows it in `charter_fb` inline. Telegram notification on successful wire; `agent_feedback` build_request (metadata.intent_type) if no match. All three "Done when" criteria verified via integration tests.

## Key Decisions
- Scoring: 80% source_preferences coverage, 20% question coverage — pref-heavy because the card's primary signal is source preferences.
- Threshold 0.7 (per card). thread_research_agent scores 0.8 against "web search".
- `intent_type` stored in `agent_feedback.metadata` JSONB, not a top-level column (B-086 classifier pattern).
- Return `{**thread, '_auto_wire': result}` from `save_charter` rather than a separate callable — cleaner for the client flow.
- Client sets `t_state[0]['bound_agent']` on auto-wire so `_build_thread_actions` reflects it immediately.

## Capability Delta
**Before:** Threads required manual agent wiring after charter creation.
**After:** Charter save auto-wires the best-matched agent (≥0.7 confidence) or queues a build request. No manual step for web-search threads.

## Lessons Written
1. agent_feedback.intent_type lives in metadata JSONB

## Branches
- attempt/b119-auto-wiring → merged to main, deleted

## Usage
~12% session / weekly TBD
