# Session: 2026-05-10-1800
Session type: lean
Directive: Run B-127
Code commit: 91b55eb (claude-dashboard master)

## Tasks completed
- B-127: Dashboard layout restructure — 10 tabs → 5-tab structure

## Key decisions
- **Inbox and Controls lifted to Home only** — System/Fleet contains status+agents+queue only. Duplicating Anvil widgets (single-parent ownership) is not possible; Home is the actionable surface, System is for periodic monitoring. This is a "scoped judgment call" per the card's invitation.
- **Lazy-load on section expand** — Extended `_make_section` with optional `on_expand` callback. Each System sub-section loads its data when first expanded, preserving the existing lazy-load pattern without eager-loading all 7 sections at once.
- **Font pass: 16→18, 20→22** — `replace_all` edit touched 86 occurrences mechanically. Status strip uses 24px for scannability.
- **Workpad stub text** — Verbatim from card: "Workpad — coming in B-128. This will be a lightweight investigation surface for exploring questions before they become threads."
- **Health indicator logic** — Green (🟢) when inbox==0, Yellow (🟡) when inbox>0. No error-log query at startup (would add latency; can extend in B-128).

## Capability delta
**Before:** Dashboard had 10 flat tabs with daily-use controls buried in Fleet/Controls.
**After:** Five-tab layout with Home as the daily-use landing surface. Home provides: health status strip with counts, note capture, inbox approvals, autonomous toggle, lean trigger, and export shortcuts. System tab consolidates all system-internal views as collapsible sections.

New: `_build_home_layout`, `_build_workpad_layout`, `_build_fleet_inner`, `_build_system_layout`, lazy loaders for 6 System sub-sections, `_update_home_status_strip`, home note/export handlers.

## Lessons written
1. `lesson_anvil_widget_single_parent_2026-05-10` — Anvil widgets have single-parent ownership; plan widget placement before building when same data needs to appear in multiple contexts.

## Branches
- `attempt/b127-five-tab-layout` → merged to master 91b55eb

## Commit hashes
- claude-dashboard: 91b55eb
- claudis: (TRAJECTORY.md + this artifact, see next commit)

## Line count
682 lines changed (357 inserted, 325 deleted), net +32. Within 300-400 estimate; font pass added ~86 mechanical replacements.

## Usage
~25% session, ~40% weekly (estimate)
