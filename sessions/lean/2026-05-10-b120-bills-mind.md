# Session: B-120 — Bill's Mind Capture and Working Bundle
Date: 2026-05-10
Type: lean
Card: B-120
Code commits: (see below — committed with this artifact)

## Tasks Completed
- Created `bill_notes` Supabase table (id uuid PK, content text, created_at, addressed bool, addressed_at)
- Added 4 uplink callables to uplink_server.py: `get_bill_notes`, `add_bill_note`, `get_working_bundle`, `mark_bill_note_addressed`
- Added Workspace tab to Anvil dashboard (Form1/__init__.py): textarea input, Add note button, live notes list with Mark addressed per-row, Export working bundle button

## Key Decisions
- Added `get_bill_notes()` alongside the three card-specified callables — the UI needs a list endpoint separate from the bundle callable.
- Placed Workspace at end of tab row (after Grader) — minimal disruption to existing layout.
- `get_working_bundle()` reads session artifacts from `~/aadp/claudis/sessions/lean/` directly (glob + file read) — no extra table needed.
- "After:" line from Capability Delta used as the one-line outcome for each artifact.
- Flagged artifacts: cutoff = 14 days, keywords = failed/stuck/blocked/unresolved.

## Capability Delta
**Before:** Bill had no place to leave general notes. Directive field was the only structured intent capture, and it's narrow. Annotations on threads didn't flow to desktop sessions.
**After:** Bill can write free-text notes via the Workspace tab. Notes persist in `bill_notes`, surface in the working bundle at session start, and can be marked addressed. Desktop Claude calls `get_working_bundle()` to read them at boot.

## Lessons Written
None new — existing patterns applied cleanly.

## Verification Checklist
- [x] `bill_notes` table created in Supabase (201 response confirmed)
- [x] `get_bill_notes`, `add_bill_note`, `get_working_bundle`, `mark_bill_note_addressed` in uplink_server.py
- [x] Workspace tab button in tab row, panel added, `_set_tab` dict updated
- [x] `_show_workspace_tab`, `_build_workspace_layout`, `_load_workspace_notes`, `_workspace_add_note_clicked`, `_workspace_export_clicked` all implemented
- [ ] Live test: add a note, mark addressed, export bundle (requires uplink restart + Anvil sync)
