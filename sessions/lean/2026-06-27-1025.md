# Session Artifact: 2026-06-27-1025

**Session type:** Lean (directive-driven)
**Directive:** B-138 — Decouple auto_cycle toggle from growth scheduler
**Code commit:** 536ab03

---

## Tasks completed

- Added `set_auto_cycle_only(enabled: bool)` callable to `anvil/uplink_server.py`
- Callable patches `system_config.auto_cycle_enabled` via Supabase REST only — no n8n call
- Existing `set_autonomous_mode` left unchanged (still controls both n8n + auto_cycle for Home tab)
- Restarted `aadp-anvil` service; uplink reconnected cleanly

## Verification evidence

```
$ python3 -c "patch_auto_cycle(False); read_auto_cycle()"
set_auto_cycle_only(False) → HTTP 204, auto_cycle_enabled = False

$ python3 -c "patch_auto_cycle(True); read_auto_cycle()"
set_auto_cycle_only(True)  → HTTP 204, auto_cycle_enabled = True

$ curl n8n /api/v1/workflows/Lm68vpmIyLfeFawa → active: False  (unchanged throughout)
```

## Key decisions

- New callable rather than a parameter on `set_autonomous_mode` — keeps the Home tab button unchanged and gives Projects tab a clean, purpose-scoped entry point
- No error list in return value (unlike `set_autonomous_mode`) — single operation, raise_for_status covers it

## Capability delta

**Before:** The only way to set `auto_cycle_enabled` was `set_autonomous_mode`, which always also toggled the growth scheduler — making it unsafe to start an autonomous project without triggering unrelated 6-hour work injections.

**After:** `set_auto_cycle_only(enabled)` patches `system_config.auto_cycle_enabled` independently. The Projects tab (B-140) can wire this to Start/Stop project actions without touching the scheduler.

**Reader:** B-140 Projects tab — "Start" button calls `set_auto_cycle_only(True)`, "Stop after current session" calls `set_auto_cycle_only(False)`.

## Lessons written

0 (no new lessons — task was a straightforward bounded callable addition)

## Branches

- `attempt/b138-set-auto-cycle-only` → merged to main → 536ab03

## Pending feedback surfaced (not acted on — both pre-date this session)

- `project_node` 2165e3d0: grader FAIL — prose description instead of verbatim Bash output
- `project_node` e9fb1b46: grader FAIL — paraphrased result instead of verbatim terminal output

## Usage

~15% session, weekly% unknown
